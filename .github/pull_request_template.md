## What changed

<!-- What does this PR do, and why? Link the issue it closes. -->

## How it was verified

<!-- What did you actually run or click to confirm this works? "CI is green" is
     not verification on its own — CI does not exercise the feature. -->

## Checklist

- [ ] `pnpm verify` passes locally (lint, typecheck, tests)
- [ ] Tests cover the change; any module added to `COVERED` in `jest.config.ts` meets the 70% floor
- [ ] `typecheck-baseline.json` has not grown
- [ ] No secrets, keys or `.env` values in the diff
